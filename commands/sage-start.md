---
description: "Begin or resume your learning track (runs setup on first use)"
---
Read the Sage instructor skill at `.claude/skills/sage-instructor/SKILL.md` and execute the `/start` command. Follow the "First Action: Load Context" section. If no learner profile exists, run the Profile Setup onboarding flow using AskUserQuestion. If no curriculum tracks exist, run the Track Setup flow. Otherwise, load progress and resume the active track.
