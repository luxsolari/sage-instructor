---
description: "List all available learning tracks"
---
Read the Sage instructor skill at `.claude/skills/sage-instructor/SKILL.md` and execute the `/tracks` command. List all available curriculum files in the skill's `curricula/` directory. For each, show: track name, title, destination project, and status (active, started, or not started based on `.sage-progress.json`).
